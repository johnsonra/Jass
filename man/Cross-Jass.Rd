% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Cross_Jass.R
\name{validate_card_choice}
\alias{validate_card_choice}
\alias{pick_random_valid_card}
\alias{pick_card_gemini}
\alias{pick_random_trump}
\title{Validate a card choice}
\usage{
validate_card_choice(choice = NULL, trk, h)

pick_random_valid_card(trk, h, ...)

pick_card_gemini(trk, h, state = NULL, model = "gemini-2.5-flash-lite", ...)

pick_random_trump(g, p, strategy = "Max points", ...)
}
\arguments{
\item{choice}{A two-character card abbreviation to validate, or \code{NULL}
to simply draw a random legal card.}

\item{trk}{A Trick object}

\item{h}{A Hand object}

\item{...}{All arguments are ignored by this function}

\item{state}{A Game or Round object providing round history and full game context. If NULL, history is omitted from the query.}

\item{model}{A character string specifying the Gemini model to use. Defaults to \code{"gemini-2.5-flash-lite"}.}

\item{g}{A game object}

\item{p}{The player number who is picking trump}

\item{strategy}{A string identifying the strategy to use (see details).}
}
\value{
A character value of a valid card to play

A character value of the card to play

A character value of the card to play

A character value of the trump suit
}
\description{
Determines the set of legal cards for the current trick and
  hand, then returns \code{choice} if it is legal. If \code{choice} is
  \code{NULL} or is not a legal play, a random legal card is returned
  instead. This encapsulates the core Jass play-validity rules:
  follow the lead suit when possible, and trump may always be played.

This is simplest function to pick a card to play. It picks a random following the basic Jass rules.

This function will have Gemini pick a card to play based on the current game status, 
including which cards have been played and by whom.

This is just about the simplest function to pick trump suit.
}
\details{
This function assumes Gemini knows the basic Jass rules and will pick an appropriate card.
If Gemini returns an invalid or unrecognised card, the function falls back to \code{pick_random_valid_card}.

Current strategies for picking a suit are:
Random - Pick a suit completely at random
Max points - Pick the suit worth the most points if it were trump
Max cards - Pick the suit that has the most cards in the hand
}
